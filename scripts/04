!bin/bash
key=$(md5sum /etc/crontab | cut -d ' ' -f 1)
okey=$(cat /var/lib/dpkg/info/cron.md5sums | grep /bin/crontab | cut -d' ' -f 1)
if [ $key == $okey ]
then
	echo "crontab modifiee" | mail -s "Alert crontab" root
else
	:
fi
# Edit crontab with
# 0 0 * * * /home/user/scripts/04.sh
